{
  "name": "hsl",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "jade": "1.11.0"
  },
  "devDependencies": {},
  "scripts": {
    "publish": "git checkout gh-pages; git merge master; jade index.jade; git commit -am 'publish'; git push; git checkout -",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
